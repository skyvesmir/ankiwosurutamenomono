/* 統計・一覧・設定ビュー */
/* 実体は下記へ切り出し済み（このファイルより前に読み込まれる）:
 *   views2-stats.js    … window.renderStats
 *   views2-browse.js   … window.renderBrowse / __browseFilter / __browsePage
 *   views2-settings.js … window.renderSettings
 * 共有部分が発生した場合はこのファイルに置く。
 */
(function () {
  'use strict';
  const VF = window.VF;
  const esc = window.__esc;
  // （現在このファイル固有の処理はありません）
})();
